/** 编辑器 UI 主题配置（`createIETMEditor({ theme })`） */
export type IETMThemeMode = "light" | "dark" | "auto";

/** 当前生效的亮色 / 暗色（`getTheme()` / `onThemeChange`） */
export type IETMResolvedTheme = "light" | "dark";
